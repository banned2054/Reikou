<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:components="clr-namespace:TestMpv.Views.Components">

    <ControlTheme x:Key="{x:Type components:VideoTransportControls}" TargetType="components:VideoTransportControls">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid RowDefinitions="Auto,Auto">
                    <Grid.Styles>
                        <Style Selector="FlyoutPresenter.VolumeFlyout">
                            <Setter Property="Background" Value="#CC000000" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="CornerRadius" Value="8" />
                            <Setter Property="MinWidth" Value="46" />
                            <Setter Property="MaxWidth" Value="46" />
                            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
                            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden" />
                        </Style>
                    </Grid.Styles>

                    <Slider Grid.Row="0" x:Name="PART_TimeSlider"
                            Margin="10,0"
                            Value="{TemplateBinding CurrentTime, Mode=TwoWay}"
                            Maximum="{TemplateBinding Duration}"
                            Minimum="0"
                            Focusable="False" />

                    <Grid Grid.Row="1" ColumnDefinitions="Auto,*,Auto" Margin="10,0,10,10">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                            <Button x:Name="PART_PreviousButton" Command="{TemplateBinding PreviousCommand}" Classes="IconButton" Width="32" Height="32">
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-previous.svg" Stretch="Uniform" />
                                </Panel>
                            </Button>
                            <Button x:Name="PART_BackwardButton" Command="{TemplateBinding BackwardCommand}" Classes="IconButton" Width="32" Height="32">
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-backward.svg" Stretch="Uniform" />
                                </Panel>
                            </Button>
                            <Button x:Name="PART_PlayPauseButton" Command="{TemplateBinding PlayPauseCommand}" Classes="IconButton" Width="32" Height="32">
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-play.svg"
                                         IsVisible="{Binding !IsPlaying, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                    <Svg Path="avares://TestMpv/Assets/ico-pause.svg"
                                         IsVisible="{Binding IsPlaying, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                </Panel>
                            </Button>
                            <Button x:Name="PART_ForwardButton" Command="{TemplateBinding ForwardCommand}" Classes="IconButton" Width="32" Height="32">
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-forward.svg" Stretch="Uniform" />
                                </Panel>
                            </Button>
                            <Button x:Name="PART_NextButton" Command="{TemplateBinding NextCommand}" Classes="IconButton" Width="32" Height="32">
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-next.svg" Stretch="Uniform" />
                                </Panel>
                            </Button>
                            <TextBlock Text="{TemplateBinding TimeText}" Foreground="White"
                                       VerticalAlignment="Center" FontSize="12" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="15" VerticalAlignment="Center">
                            <Button x:Name="PART_VolumeButton" Command="{TemplateBinding ToggleMuteCommand}" Classes="IconButton" Width="32" Height="32">
                                <Button.Flyout>
                                    <Flyout Placement="Top" FlyoutPresenterClasses="VolumeFlyout">
                                        <Grid Width="46" Height="130">
                                            <Slider Orientation="Vertical" Height="100"
                                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                                    Minimum="0" Maximum="200"
                                                    Value="{Binding Volume, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                                <Slider.RenderTransform>
                                                    <TranslateTransform X="-2" />
                                                </Slider.RenderTransform>
                                            </Slider>
                                        </Grid>
                                    </Flyout>
                                </Button.Flyout>
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-volume-off.svg"
                                         IsVisible="{Binding IsMuted, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                    <Svg Path="avares://TestMpv/Assets/ico-volume.svg"
                                         IsVisible="{Binding !IsMuted, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                </Panel>
                            </Button>
                            <Button x:Name="PART_DanmakuButton" Command="{TemplateBinding ToggleDanmakuCommand}" Classes="IconButton" Width="32" Height="32">
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-danmaku-switch-on.svg"
                                         IsVisible="{Binding !IsPlaying, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                    <Svg Path="avares://TestMpv/Assets/ico-danmaku-switch-off.svg"
                                         IsVisible="{Binding IsPlaying, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                </Panel>
                            </Button>
                            <Button
                                Command="{TemplateBinding ChangeSpeedCommand}"
                                Content="{Binding PlaybackSpeed, RelativeSource={RelativeSource TemplatedParent}, StringFormat=\{0:0.0\}x}"
                                Background="Transparent" Foreground="White" Focusable="False" FontWeight="Bold"
                                Padding="5" />
                            <Button x:Name="PART_FullscreenButton" Command="{TemplateBinding ToggleFullscreenCommand}" Classes="IconButton" Width="32" Height="32">
                                <Panel>
                                    <Svg Path="avares://TestMpv/Assets/ico-fullscreen.svg"
                                         IsVisible="{Binding !IsFullscreen, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                    <Svg Path="avares://TestMpv/Assets/ico-fullscreen-exit.svg"
                                         IsVisible="{Binding IsFullscreen, RelativeSource={RelativeSource TemplatedParent}}"
                                         Stretch="Uniform" />
                                </Panel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Grid>
            </ControlTemplate>
        </Setter>
    </ControlTheme>

</ResourceDictionary>
